using UnityEngine;
using System.Collections;

public class GoalObject : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	//	transform.localRotation = Quaternion.AngleAxis(Time.deltaTime * 90.0f, Vector3.up);
	}
}
